{
  "name": "dispipe-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter \"*\" build",
    "test": "bun test --filter \"*\"",
    "release": "bun run build && bun run --filter \"*\" publish"
  },
  "dependencies": {
    "@types/node": "^25.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
